spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        service-id: config-server
#      uri: http://localhost:7010/

eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/
